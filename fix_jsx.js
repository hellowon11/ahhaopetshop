const fs = require("fs"); const filePath = "src/pages/GroomingAppointment.tsx"; fs.readFile(filePath, "utf8", (err, data) => { if (err) { console.error("Error reading file:", err); return; } const fixedContent = data.replace(/    <\/div>\n  \);/g, "    </div>\n    </>\n  );"); fs.writeFile(filePath, fixedContent, "utf8", (err) => { if (err) { console.error("Error writing file:", err); return; } console.log("File successfully fixed!"); }); });
